apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: track-siamrpnpp-build

build:
  tagPolicy:
    inputDigest: {}
  local:
    push: false
    useDockerCLI: true
    useBuildkit: true
  artifacts:
    - image: track-siamrpnpp
      context: .
      docker:
        dockerfile: runtime.Dockerfile
      sync:
        manual:
          - src: "app/**/*.py"
            dest: /workspace
          - src: "pysot/**/*.py"
            dest: /workspace
          - src: "configs/**/*.yaml"
            dest: /workspace
      hooks:
        after:
          - command: ["sh", "-c", "./after_build_update.sh"]
            os: [darwin, linux]

profiles:
  - name: minikube
    activation:
      - kubeContext: minikube
